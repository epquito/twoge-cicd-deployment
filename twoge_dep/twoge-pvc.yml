apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: twoge-vpc
  namespace: <Namespace name>
spec:
  storageClassName: twoge-storage
  resources:
    requests:
      storage: 2Gi
  accessModes:
    - ReadWriteOnce
