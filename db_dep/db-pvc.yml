apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-vpc
  namespace: <Namespace name>
spec:
  storageClassName: db-storage
  resources:
    requests:
      storage: 2Gi  
  accessModes:
    - ReadWriteOnce
